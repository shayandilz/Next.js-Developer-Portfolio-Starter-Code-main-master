import React from 'react';
import Layout from "@/components/Layout";

const Footer = () => {
    return (
        <footer className={'w-full border-t-2 border-dark border-solid dark:border-light'}>
            <Layout className={'py-8 flex justify-center items-center dark:text-light'}>
                DUDE PUT WEATHER AND LIVE CLOCK DOWN HERE LATER
            </Layout>
        </footer>
    );
};

export default Footer;